import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    background: {
      default: '#f3e5f5',  // very light violet
    },
    primary: {
      main: '#7b1fa2', // Violet but more violeter
    },
  },
});
